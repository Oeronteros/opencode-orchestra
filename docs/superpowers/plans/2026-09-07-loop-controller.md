# Bounded Loop Implementation Plan

1. Inspect current partial implementation and installed plugin/SDK signatures.
2. Replace protocol and controller with explicit activation, scoped permissioned plan instructions, bounded state, cancellation and honest completion claims.
3. Wire command lifecycle, assistant parent correlation, idle gating and nonblocking orch-lead promptAsync; preserve ordinary sessions.
4. Add regression tests and synchronize README, JSON schema and design specification.
5. Run typecheck and full tests, inspect diff and report limitations. Do not commit or revert unrelated changes.
